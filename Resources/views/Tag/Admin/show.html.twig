{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
<h1>{{ 'ekyna_demo.tag.header.show'|trans({'%name%': tag.name})|raw }}</h1>
{% endblock %}

{% block actions %}
{{ admin_resource_btn(tag, 'list') }}
{{ admin_resource_btn(tag, 'new') }}
{% endblock %}

{% block main %}
<div class="actions">
    {{ admin_resource_btn(tag, 'edit') }}
    {{ admin_resource_btn(tag, 'remove') }}
</div>
<div class="show show-with-tabs">
    <ul class="nav nav-tabs">
	    <li class="active"><a href="#prod-tag-general" data-toggle="tab">{{ 'ekyna_core.field.general'|trans }}</a></li>
	</ul>
	<div class="show-body">
	    <div class="tab-content">
		    <div class="tab-pane active" id="prod-tag-general">
		      {{ show_row(tag.name, null, 'ekyna_core.field.name') }}
		    </div>
	    </div>
	</div>
</div>
{% endblock %}

{% block side %}
{% endblock %}