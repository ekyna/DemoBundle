{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
<h1>{{ 'ekyna_demo.tag.header.remove'|trans({'%name%': tag.name})|raw }}</h1>
{% endblock %}

{% block actions %}
{{ admin_resource_btn(tag, 'list') }}
{{ admin_resource_btn(tag, 'new') }}
{% endblock %}

{% block main %}
<form action="{{ path('ekyna_demo_tag_admin_remove', {'tagId': tag.id}) }}" method="post" class="form-horizontal">
    <div class="form-body">
	    {{ bootstrap_set_style('horizontal') }}
		{{ form_widget(form) }}
    </div>
    {{ ui_form_footer(form) }}
</form>
{% endblock %}

{% block side %}
{% endblock %}